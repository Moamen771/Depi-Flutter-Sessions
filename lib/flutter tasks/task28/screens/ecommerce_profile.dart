import 'package:flutter/material.dart';

class EcommerceProfile extends StatelessWidget {
  const EcommerceProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Profile Screen'),
      ),
    );
  }
}
