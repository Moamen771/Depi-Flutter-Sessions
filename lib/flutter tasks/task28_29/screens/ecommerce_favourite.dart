import 'package:flutter/material.dart';

class EcommerceFavourite extends StatelessWidget {
  const EcommerceFavourite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Favourite Screen'),
      ),
    );
  }
}
